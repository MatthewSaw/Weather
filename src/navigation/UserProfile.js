import React, { PureComponent } from "react";
import { View, TouchableOpacity, Text, FlatList, SafeAreaView, ScrollView } from "react-native";


class UserProfile extends PureComponent{

  render() {
    return(
      <View>
      </View>
    );
  }
}

export default UserProfile;
